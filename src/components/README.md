# components/

Shared UI components used across the three presentation surfaces (`/dashboard`, `/m/[merchantSlug]`,
`/superadmin` — see `FINAL-ARCHITECTURE.md` §2 and §31).

Bootstrap stage: empty boundary, no components implemented yet. Do not add domain-specific components
(points, rewards, coupons, ...) before the corresponding Phase is approved.
